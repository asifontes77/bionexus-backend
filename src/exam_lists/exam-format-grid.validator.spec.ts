import { BadRequestException } from '@nestjs/common';
import { validateExamFormatGrid } from './exam-format-grid.validator';
const field={key:'resultado',label:'Resultado',source:'text',formula:'',formulaMode:'single',selectorFieldKey:'',cases:{},rules:[],defaultResult:{type:'empty',value:''},dataType:'text',decimals:0,options:[],cell:'B1'};
const valid:any={version:2,layout:{logicalWidth:780,heightMode:'content'},grid:{data:[['Resultado','{{resultado}}','','']],columns:[195,195,195,195],rows:[32],merges:{},styles:{}},fields:{resultado:field},modules:{}};
describe('validateExamFormatGrid',()=>{it('normaliza formato valido',()=>expect(validateExamFormatGrid(valid)).toEqual({format:valid,size:1}));it('admite null',()=>expect(validateExamFormatGrid(null)).toEqual({format:null,size:1}));it('admite antibiograma',()=>{const v=structuredClone(valid);v.fields={};v.modules={A1:{type:'antibiogram',version:1}};v.grid.merges={A1:[4,1]};expect(validateExamFormatGrid(v).format).toEqual(v)});it.each([undefined,{}, {...valid,version:1},{...valid,grid:{...valid.grid,columns:[780]}}])('rechaza contrato invalido',(v)=>expect(()=>validateExamFormatGrid(v)).toThrow(BadRequestException));it('rechaza campo y modulo en la misma celda',()=>{const v=structuredClone(valid);v.modules={B1:{type:'antibiogram',version:1}};expect(()=>validateExamFormatGrid(v)).toThrow('EXAM_CATALOG_GRID_FORMAT_INVALID')})});
