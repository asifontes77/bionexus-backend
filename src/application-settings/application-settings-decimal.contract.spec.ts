import * as fs from 'fs';import * as path from 'path';const r=(p:string)=>fs.readFileSync(path.join(process.cwd(),p),'utf8');describe('decimal separator contract',()=>{const all=r('src/application-settings/application-settings.entity.ts')+r('src/application-settings/dto/update-application-settings.dto.ts')+r('src/application-settings/application-settings.service.ts')+r('src/database/migrations/1788753600000-ApplicationSettingsDecimalSeparator.ts');it('persists and validates comma or point',()=>{expect(all).toContain('decimal_separator');expect(all).toContain("decimal_separator:[',','.']");expect(all).toContain("default: ','")});it('is reversible',()=>expect(all).toContain('DROP COLUMN decimal_separator'))});
