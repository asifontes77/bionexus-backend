import * as fs from 'fs';import * as path from 'path';const read=(f:string)=>fs.readFileSync(path.join(process.cwd(),f),'utf8');describe('Application Settings contract',()=>{const controller=read('src/application-settings/application-settings.controller.ts'),service=read('src/application-settings/application-settings.service.ts'),migration=read('src/database/migrations/20260827235000-ApplicationSettingsPermissions.ts');it('protects read and update',()=>{expect(controller).toContain("@RequirePermissions('application-settings.read')");expect(controller).toContain("@RequirePermissions('application-settings.update')")});it('projects exactly nine settings fields',()=>{for(const f of ['voucher_format','receipt_format','head_html','body_html','page_html','maximum_rows_report','workshee_format','printer_type','printer_interface'])expect(service).toContain(f)});it('uses transaction and audit',()=>{expect(service).toContain('this.dataSource.transaction');expect(service).toContain("action: 'application-settings.updated'")});it('creates idempotent permissions for admin',()=>{expect(migration).toContain('ON DUPLICATE KEY UPDATE');expect(migration).toContain('INSERT IGNORE INTO security_role_permissions')})});