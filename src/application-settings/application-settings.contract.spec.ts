import * as fs from 'fs';import * as path from 'path';const read=(f:string)=>fs.readFileSync(path.join(process.cwd(),f),'utf8');describe('Application Settings multi laboratory contract',()=>{const entity=read('src/application-settings/application-settings.entity.ts'),service=read('src/application-settings/application-settings.service.ts'),migration=read('src/database/migrations/1787877300000-ApplicationSettingsPerLaboratory.ts');it('belongs to one laboratory',()=>{expect(entity).toContain('laboratory_id');expect(entity).toContain("@JoinColumn({ name: 'laboratory_id' })");expect(migration).toContain('UQ_application_settings_laboratory');expect(migration).toContain('FK_application_settings_laboratory')});it('contains session configuration',()=>{for(const field of ['session_timeout_minutes','inactivity_timeout_minutes','countdown_seconds'])expect(entity+service).toContain(field)});it('migrates all current laboratory values',()=>{expect(migration).toContain('SELECT id,30,20,120,voucher_format');expect(migration).not.toContain('ALTER TABLE laboratory DROP')});it('uses transaction and audit',()=>{expect(service).toContain('this.dataSource.transaction');expect(service).toContain("action:'application-settings.updated'")})});