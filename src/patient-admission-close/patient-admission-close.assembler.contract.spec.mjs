import fs from 'node:fs';import path from 'node:path';const source=fs.readFileSync(path.join(process.cwd(),'src/patient-admission-close/patient-admission-close.assembler.ts'),'utf8');for(const token of ['patient_profile_id:0','patientsId:0','id_way_pay:0',"action:'patient-admission.closed'",'total_canceled:totals.paid'])if(!source.includes(token))throw new Error('ASSEMBLER_'+token);console.log('[OK] Ensamblador persistente aprobado sin escritura real.');