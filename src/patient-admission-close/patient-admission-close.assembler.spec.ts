import { PatientAdmissionCloseAssembler } from './patient-admission-close.assembler';
describe('PatientAdmissionCloseAssembler',()=>{const assembler=new PatientAdmissionCloseAssembler();const input:any={clientId:1,tariffId:2,patient:{name:'Paciente',age:30,ageUnit:'Años',sex:false,sample:'Tomada aquí'},exams:[{examCatalogId:7,quantity:1}],payments:[{typePaymentId:3,amount:10,currency:'BASE',fieldValues:[]}]};const resolved:any={client:{business_name:'Ambulatorio'},exams:[{examCatalogId:7,description:'Hemograma',groupId:1,position:2,taxDescription:'IVA'}]};const exams:any=[{examCatalogId:7,quantity:1,unitPrice:10,subtotal:10,taxRate:16,taxTotal:1.6,total:11.6}];const payments:any=[{typePaymentId:3,currency:'BASE',currencyId:1,enteredAmount:11.6,baseAmount:11.6,localAmount:464,fieldValues:[]}];const totals={subtotal:10,taxTotal:1.6,total:11.6,paid:11.6,balance:0};
it('ensambla ingreso, examen y pago sin ids anticipados',()=>{const value=assembler.build(input,resolved,exams,payments,totals,5,new Date('2026-09-10T20:00:00.000Z'));expect(value.admission).toMatchObject({patient_profile_id:0,client_id:1,tariff_id:2,total:11.6,user_id:5});expect(value.exams[0]).toMatchObject({patientsId:0,exam_catalog_id:7,total:11.6});expect(value.wayPayItems[0]).toMatchObject({id_way_pay:0,id_type_payment:3,enteredAmount:11.6,baseAmount:11.6,localEquivalentAmount:464,currencyId:1});});
it('prepara auditoria sin datos sensibles',()=>{const value=assembler.build(input,resolved,exams,payments,totals,5);expect(value.audit).toMatchObject({action:'patient-admission.closed',metadata:{examCount:1,paymentCount:1,total:11.6}});expect(JSON.stringify(value.audit)).not.toContain('document_number');});
it('rechaza actor invalido',()=>expect(()=>assembler.build(input,resolved,exams,payments,totals,0)).toThrow('PATIENT_ADMISSION_CLOSE_ACTOR_INVALID'));});
